package Parse

import TTS "../Structure"

func ParseKIB(subject, typeOfLesson, teacherName, cabinet, dayOfWeek, numberLesson, week string) []TTS.Lesson {
	return []TTS.Lesson{TTS.NewLesson(), TTS.NewLesson()}
}
